# =============================================================================
# MedEval — Python dependencies
# =============================================================================
# Install with:
#   pip install -r requirements.txt
#
# For GPU with quantisation support, also install bitsandbytes:
#   pip install bitsandbytes>=0.43.0
#
# If using a CUDA environment, ensure your torch version matches your CUDA version.
# Install torch separately from https://pytorch.org/get-started/locally/
# =============================================================================

# Core ML
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.29.0          # Required for device_map="auto"
datasets>=2.19.0            # HuggingFace datasets
huggingface-hub>=0.22.0     # Model downloads and caching
tokenizers>=0.19.0

# Quantisation (optional but recommended for large models on single GPU)
bitsandbytes>=0.43.0

# Evaluation and numerical
numpy>=1.26.0
bert-score>=0.3.13 

# Config
pyyaml>=6.0.1

# Utilities
tqdm>=4.66.0                # Progress bars in HuggingFace internals

# pip install sentencepiece tiktoken